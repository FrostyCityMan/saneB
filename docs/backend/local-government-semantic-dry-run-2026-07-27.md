# 지자체 수집 의미 분류 Dry-run

## 1. 실행 기준

- 기준 시각: 2026-07-27 18:18 KST 이후
- 기준 운영 실행: `ASRUN-000009`
- 자동수집: 변경 전 일시중지
- 운영 출처: 244곳
- 사용 중 출처: 21곳
- 기존 `LOCAL_GOV_NOTICE` 검수대기 원문: 293건
- 처리 원칙: 유효 원문과 운영 이력 보존, 파괴적 삭제 금지

조회 전용 재현 SQL은
`scripts/operations/dry-run-local-government-semantic-classification.sql`이다.

## 2. 244개와 226개 차이

현재 244개 출처는 단일한 “시·군·구 수”가 아니다.

| 행정 단위 | 건수 |
|---|---:|
| 기초지자체 | 227 |
| 시·도 | 15 |
| 행정시 | 2 |
| 합계 | 244 |

클라이언트의 226개는 시·군·구만 가리키는 운영 표현으로 볼 수 있으나,
현재 정적 seed는 시·도와 행정시를 함께 관리한다. 숫자를 DB 제약으로
고정하지 않고 행정 단위 유형으로 구분한다.

## 3. 출처 분류 결과

V56과 동일한 공식 URL·페이지 유형 정적 규칙을 적용한 결과다.

| 게시판 유형 | 수집 정책 | 건수 |
|---|---|---:|
| 고시·공고 | 전체 수집 | 25 |
| 지원·모집 | 전체 수집 | 10 |
| 일반 공지 | 키워드 선별 | 208 |
| 보도자료 | 수집 제외 | 1 |
| 합계 |  | 244 |

기존 사용 중 출처 21곳 중 경기도청 `LGS-000084`는 보도자료 URL이었다.
V56 적용 시 이 출처를 제외·중지하고 실제 수집 대상은 20곳이 된다.
검단구청 `LGS-000063`은 2026-07-01 구 신설 이후 기존 공지 URL이
404로 전환되어 공식 고시공고 URL로 보정했다. 서버군의 세션 고정이
필요해 기존 휴리스틱 파서와 `SESSION_BROWSER` 전송 프로필을 사용한다.

## 4. 전수 QA 결과

- 실행일: 2026-07-27
- 대상: 244곳
- 파서 QA: `PASS` 244, 미통과 0
- 의미 검증: `MATCHED` 35, `KEYWORD_FILTER_REQUIRED` 208, `EXCLUDED` 1
- `SEMANTIC_MISMATCH`, `REVIEW_REQUIRED`, `NOT_EVALUATED`: 0
- 운영 DB 수집: 없음

연결·파싱 결과와 게시판 의미 결과는 별도 열로 기록했다. 파서가
통과해도 보도자료는 `EXCLUDED`로 유지하며, 일반 공지는 항목별 정적
키워드 판정을 거친다.

## 5. 기존 293건 Dry-run

정적 포함·제외 키워드와 출처 정책을 적용한 결과다.

| 판정 | 건수 |
|---|---:|
| 유효 후보 `ACCEPTED` | 144 |
| 별도 확인 `REVIEW_REQUIRED` | 0 |
| 수집 제외 `EXCLUDED` | 149 |
| 합계 | 293 |

| 판정 사유 | 건수 |
|---|---:|
| 일반 공지 포함 키워드 일치 | 134 |
| 고시·공고/지원 게시판 전체 수집 | 10 |
| 일반 공지 포함 키워드 없음 | 124 |
| 일반 공지 제외 키워드 일치 | 13 |
| 보도자료 출처 정책 제외 | 12 |

## 6. 적용 정책

- `ACCEPTED`와 `REVIEW_REQUIRED`만 기본 검수대기 목록에 포함한다.
- `EXCLUDED`는 운영 공고와 연결되지 않은 경우 `ARCHIVED`로 이관한다.
- 운영 공고와 이미 연결된 원문은 자동 삭제하거나 연결을 끊지 않는다.
- 수집 실행, URL별 결과, 감사 로그는 보존한다.
- QA 산출물 전체 삭제 API는 이번 작업에서 호출하지 않는다.
- 시험 데이터 정리는 별도 식별 근거가 있는 경우에만 수행한다.

## 7. 배포 후 확인 항목

1. V56 적용 전후 출처와 원문 건수를 다시 집계한다.
2. 보도자료·미검증·수집 제외 출처가 사용 상태인지 확인한다.
3. 기존 293건의 실제 `ACCEPTED`, `REVIEW_REQUIRED`, `EXCLUDED` 결과를 확인한다.
4. 운영 공고 연결 원문이 보존됐는지 확인한다.
5. 관리자 기본 검수대기 목록에서 제외 자료가 보이지 않는지 확인한다.
